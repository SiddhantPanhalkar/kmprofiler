### kmprofiler: iOS Export Profile

**Export surface:** 55 Kotlin declarations exported to Objective-C.
**No direct Swift call site found for 18 of them.**

> This report identifies review candidates, not proven dead code or safe
> optimizations. A declaration without a textual Swift call site may still be
> used via dependency injection, protocol conformance, dynamic dispatch,
> Objective-C consumers, tests, or downstream framework consumers.

> Kotlin visibility changes (`internal`, `private`) alter the Kotlin API and
> remove the declaration from Objective-C export. Declaration-level
> `@HiddenFromObjC` preserves Kotlin visibility but removes eligible
> declarations from Objective-C/Swift export. Neither is automatically safe
> merely because a Swift text search found no call site.

#### Provenance
- **Header:** (test fixture)
- **Swift files scanned:** 2
- **Swift lines scanned:** (deterministic)
- **Scan coverage:** This scanner covers only the configured Swift source directories.
  It does not cover Objective-C files, tests, generated Swift, or downstream consumers.
- **Ownership analysis:** Heuristic-based (no Kotlin compiler metadata).
  All remediation recommendations are `MANUAL_REVIEW_ONLY` until KLIB integration is available.

#### Your code — review candidates (6)

No direct call site found in scanned Swift sources.

| Declaration | Kind | Members | Confidence | Remediation | Evidence |
|---|---|---|---|---|---|
| `SharedHomeViewModel` | class | 5 | high | manual review only | type_reference @ ContentView.swift:1 |
| `SharedHomeUiState` | class | 1 | high | manual review only | type_reference @ ContentView.swift:6 |
| `SharedCameraState` | class | 2 | high | manual review only | none |
| `SharedReviewState` | class | 2 | high | manual review only | none |
| `SharedFilterRecipe` | class | 2 | high | manual review only | none |
| `SharedCurvePoint` | class | 2 | high | manual review only | none |

**Decision tree for each candidate:**

1. **Is this declaration intentionally part of the public Kotlin API?**
   - Yes → keep it public. If Apple consumers must not see it, consider
     declaration-level `@HiddenFromObjC` and inspect every exported API
     that references it.
   - No → continue.
2. **Is it used across Kotlin files/source sets within the same module?**
   - Yes → `internal` may be appropriate, subject to the public-API graph check.
   - No, only in the same file/class → `private` may be appropriate.
3. **Does any public/protected declaration expose or inherit it?**
   - Yes → do not change only this declaration. Decide whether to
     hide/internalize the complete API chain or keep it public.
   - No → apply the selected visibility change and compile all affected targets.
4. **Is it a top-level function/property in a generated `*Kt` facade?**
   - Annotate eligible individual declarations with `@HiddenFromObjC`,
     not the file. If even one top-level declaration is needed by Swift,
     keep that declaration exported.
5. **Is ownership external or unresolved?**
   - Do not suggest editing dependency source. Report the dependency/export
     path for review.

After changes: re-link the same release framework, rerun this task, and
confirm the expected header delta.

> Declaration-level `@HiddenFromObjC` example:
> ```kotlin
> @OptIn(kotlin.experimental.ExperimentalObjCRefinement::class)
> @kotlin.native.HiddenFromObjC
> fun kotlinOnlyOperation() = Unit
> ```
> The exact opt-in/import form must be compiled against every Kotlin version
> supported by the plugin.

> 💡 Seeing names you didn't write (e.g. SDK types like `SkikoCanvas` or `ModelsCustomerInfo`)?
> Those are likely from libraries. Add their name prefix to `externalPrefixes`
> in your `kmprofiler {}` block and they will move to the 'Unresolved ownership'
> section on next run:
> ```kotlin
> kmprofiler {
>     externalPrefixes.addAll("Skiko", "Models")
> }
> ```

#### Kotlin file facades — review candidates (3)

These `*Kt` classes are generated by Kotlin/Native for source files containing
top-level functions (e.g. `ColorKt` wraps `Color.kt`).

If iOS never uses these functions directly, review eligible individual
declarations for declaration-level `@HiddenFromObjC` or visibility changes.
Do not apply file-level annotations — `@HiddenFromObjC` targets classes,
functions, and properties, not files.

| Declaration | Members | Confidence | Remediation | Evidence |
|---|---|---|---|---|
| `SharedColorKt` | 3 | high | manual review only | none |
| `SharedDimensKt` | 3 | high | manual review only | none |
| `SharedDateUtilsKt` | 2 | high | manual review only | none |

#### Unresolved ownership — 3 declarations

These declarations' names match Kotlin/Native cross-module ObjC name mangling
(`ModuleName_submoduleTypeName`) or a user-configured external prefix. Ownership
is inferred from naming heuristics, not metadata.

Do not attempt to edit these as if they were your code. Instead:
- Ensure `transitiveExport = false` (already correct if shown in Config below)
- Remove any explicit `export(deps.someLibrary)` from your `binaries.framework {}` block
- Re-link the framework and confirm the header delta

| Declaration | Members | Confidence | Evidence |
|---|---|---|---|
| `Ktor_client_coreHttpClient` | 1 | high | none |
| `Koin_coreModule` | 1 | high | none |
| `Kotlinx_coroutines_coreCoroutineScope` | 0 (empty) | high | none |

#### Config
- ✅ `transitiveExport = false` (configured in kmprofiler) — correct default.
- ℹ️ `isStatic = true` (configured in kmprofiler) — app-linker dead-stripping applies; raw framework size will substantially overstate app delta.
- ✅ 1 framework declared to the profiler; no cross-framework type duplication detected.
